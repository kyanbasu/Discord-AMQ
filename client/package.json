{
  "name": "getting-started-activity",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pm2": "pm2 start \"vite\" --name amq-client -o ./out.log -e ./err.log",
    "tunnel": "cloudflared tunnel --url http://localhost:5173"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vite-plugin-handlebars": "^2.0.0"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.0.0",
    "@sentry/browser": "^9.33.0",
    "socket.io-client": "^4.7.5"
  }
}
